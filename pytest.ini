# pytest.ini
[pytest]
addopts = -ra
testpaths =
    tests/unit
    tests/integration
markers =
    timeout(seconds): per-test timeout in seconds (requires pytest-timeout)
# Optional: set a default timeout for all tests (can still override per-test)
timeout = 20
